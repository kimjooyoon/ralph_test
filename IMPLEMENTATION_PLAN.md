# IMPLEMENTATION_PLAN — dsl-maker

> Ralph loop: complete unchecked tasks (`- [ ]`), then mark them `[x]`.

## Tasks

- [x] Bootstrap `Echo` with a domain unit test
- [x] Add `Join(a, b string) string` that returns `a + ":" + b` with tests
- [ ] Add `Split(s, sep string) []string` that splits `s` by `sep` with tests
- [ ] Add `Repeat(s string, n int) string` that repeats `s` `n` times with tests
- [ ] Add `Trim(s string, cutset string) string` that trims `s` of `cutset` with tests
