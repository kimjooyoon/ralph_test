# Implementation

## specs
- [ ] Implement `Split` with empty separator behavior for UTF-8 byte splitting (test `Split("中文", "")` should return `["中", "文"]`)
- [ ] Add `GenerateCode(pattern string) string` to return AI-style code snippets (test `GenerateCode("for i in range(5): print(i)")` should return the