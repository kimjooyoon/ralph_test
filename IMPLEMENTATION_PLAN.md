# Implementation

## specs
- [ ] Implement `Split` with empty separator behavior for UTF-8 (split by bytes, not code points)
- [ ] Add test coverage for surrogate pairs in `Split`
- [ ] Add test for empty input in `Split`
- [ ] Implement `GenerateCode(pattern string) string` (pure function returning AI-style code