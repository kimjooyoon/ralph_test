# IMPLEMENT

## specs
- [ ] Implement `Split` to split strings by UTF-8 bytes (handle multi-byte characters like Chinese)
- [ ] Add `GenerateCode(pattern string) string` for AI-style code snippets (pure function, no I/O)
- [ ] Implement `Average(nums []int) int` for mean calculation (floor)
- [ ] Add `DecodeImage(data string) []byte` for image decoding simulation
- [ ] Implement `Eval(code string) string` for code evaluation (mocked)

## Questions
- What is the expected behavior for `Split` when