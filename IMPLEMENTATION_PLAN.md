# Implementation

## specs
- [ ] Implement `Split` to correctly split UTF-8 bytes for multi-byte characters (Chinese, emoji, surrogate pairs)
- [ ] Add `EncodeBase64` to convert byte slices to Base64 strings
-