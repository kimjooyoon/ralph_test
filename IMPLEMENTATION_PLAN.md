# 
- [ ] Implement `Split` with UTF-8 byte splitting (fix `domain/split.go` to handle multi-byte characters)
- [ ] Add comprehensive UTF-8 byte splitting tests for Chinese characters, surrogate pairs, and combining marks in `domain/split_test.go`